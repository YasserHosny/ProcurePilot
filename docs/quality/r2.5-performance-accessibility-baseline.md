# R2.5 Performance and Accessibility Baseline

Date: 2026-09-14

Author: OpenCode orchestrator baseline (measured on `main` at `1ec1aa0`, branch
`codex/wave15-r2-5-planning`)

Purpose: record the measured starting point for every R2.5 performance and accessibility budget,
so success is a diff against evidence, not opinion (research R10). No code was changed to produce
this document.

## 1. Measured build state

Commands run (Wave 15 gate):

```bash
pnpm --filter web build   # succeeded, with warnings listed below
pnpm --filter web lint    # "All files pass linting." — clean
```

Full build log retained at the time of measurement; numbers below are from that run.

### Initial bundle budget — EXCEEDED

| Metric | Value | Budget | Status |
|---|---|---|---|
| Initial total (raw) | 588.35 kB | 500.00 kB | exceeded by 88.34 kB |
| Initial total (transfer) | 148.13 kB | — | informational |

This is a warning today, not an error. R2.5 task T029 (route-level lazy loading) must bring the
raw initial bundle to or under 500 kB. The budget is not raised (research R10).

### Component style budgets — 18 components exceed the 4 kB budget

| Component | Total | Over budget by |
|---|---|---|
| quotations/quotation-review | 11.94 kB | 7.94 kB |
| offers/basket-split | 11.90 kB | 7.91 kB |
| offers/compare | 9.63 kB | 5.63 kB |
| matching/match-resolution | 8.68 kB | 4.68 kB |
| catalogue/import-wizard | 8.00 kB | 4.00 kB |
| quotations/upload | 6.65 kB | 2.65 kB |
| matching/resolution-queue | 5.64 kB | 1.64 kB |
| savings/saving-evidence | 5.46 kB | 1.46 kB |
| alerts/alerts-inbox | 5.11 kB | 1.11 kB |
| quotations/review-queue | 5.07 kB | 1.07 kB |
| savings/export-savings | 4.89 kB | 0.89 kB |
| savings/savings-ledger | 4.71 kB | 0.71 kB |
| layout/shell | 4.51 kB | 0.51 kB |
| onboarding/plan-display | 4.56 kB | 0.56 kB |
| layout/home | 4.17 kB | 0.17 kB |
| team | 4.11 kB | 0.11 kB |
| offers/product-intelligence | 4.10 kB | 0.10 kB |
| catalogue/product-list | 4.04 kB | 0.04 kB |

R2.5 task T030 must bring every one of these inside budget so the production build emits zero
budget warnings (SC-005). The earlier task list draft named nine of these; the full measured set
is above and T030 references this document as its scope.

### Other build warnings (pre-existing hygiene debt, not budget violations)

- 28 x TS-998113 "X is not used within the template" warnings (unused imports of NgIf, NgForOf,
  DecimalPipe, NgClass, RouterLink, RoleDirective, FormatMoneyPipe, FormatDatePipe across
  alerts-inbox, price-history-chart, product-intelligence, plan-display, export-savings,
  outcome-capture, saving-evidence, savings-ledger).
- 7 x NG8011 MatButton icon-slot projection warnings.

Classification: pre-existing on `main`; not part of any current budget. Decision for Wave 18:
either clean up during T029/T030 work (preferred, low risk) or record as accepted debt with
owner and reason. They do not block any R2.5 gate as written.

## 2. High-value pages for accessibility regression coverage

The mandatory surface list (research R11, FR-026), all in English and Arabic:

1. Sign-in
2. Dashboard (home)
3. Smart Compare
4. Basket Split
5. Alerts Inbox
6. Supplier Scorecard
7. Savings Ledger
8. Approval Queue
9. Reports Center (new in R2.5)
10. Digest Settings (new in R2.5)

Current state: the axe suite already covers prior releases' screens (the R2.4 record shows
`compare`, `basket-split`, `supplier-scorecard`, `alerts-inbox` in the a11y run); R2.5 task T033
extends the suite to the full list above plus keyboard-only journeys for the two new surfaces and
a print-stylesheet check for the Reports Center.

## 3. Candidate performance budgets for R2.5

| Budget | Value | Source | Task |
|---|---|---|---|
| Initial bundle (raw) | <= 500 kB | angular.json budget, existing | T029 |
| Component style budget | 4 kB each, zero warnings | angular.json, existing | T030 |
| Compare-grid recalculation | < 150 ms | constitution quality gates; currently unmeasured in CI | T031 |
| Export generation, 10,000 rows | <= 60 s | R2.5 proposal (research R10) | T032 |
| Digest generation per subscription | <= 30 s | R2.5 proposal (research R10) | T024 acceptance |

The compare-grid gate is a constitution threshold with no automated regression test today — T031
closes that gap with a repeated-measurement median to stay stable against CI noise.

## 4. Screenshot and documentation risks for print and mobile readability

- The existing savings-ledger PDF renderer draws truncated raw text lines (`line[:115]`) with no
  table structure, page headers, or totals — printed output is not board-ready. The product
  critique (Finding 3) requires structured table layouts and human-readable identifiers; this is
  the same defect seen from the documentation side.
- No print stylesheet exists for any screen; the Reports Center artifact list needs one (T033),
  RTL-safe via logical properties.
- Mobile documentation (docs audit) must state that reporting surfaces are web-only in R2.5;
  digest deep links opened on mobile land on web routes.
- Screenshots for T040 must be captured in both locales, including an Arabic PDF artifact and the
  Reports Center in RTL.

## 5. Pre-existing versus R2.5 tasks

| Finding | Status on `main` | R2.5 disposition |
|---|---|---|
| Initial bundle over budget | pre-existing | T029 (lazy loading; budget not raised) |
| 18 component style overages | pre-existing | T030 (scope = the table above) |
| TS-998113 / NG8011 warnings | pre-existing | Wave 18 decision during T029/T030; not a gate |
| Compare-grid timing unmeasured | gap | T031 (new regression test) |
| Axe surface list incomplete | gap | T033 (full mandatory list, EN + AR) |
| Export timing unmeasured | gap | T032 (10k-row budget test) |
| PDF print quality | pre-existing defect | T015 (structured tables, i18n labels, Arabic shaping) |

## Gates run for this lane

- `pnpm --filter web build` — completed with the warnings recorded above.
- `pnpm --filter web lint` — passed clean.
- This document exists and is non-empty.

Open questions: none blocking. The only judgement call recorded here is the TS-998113/NG8011
disposition (clean up versus accept), which is a Wave 18 decision.
