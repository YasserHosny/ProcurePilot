# R4.1 Release Evidence

Date: 2026-09-21  
Release: Supplier IQ v2 and negotiation briefs  
Gate posture: G3 remains unmet

## Release Decision

R4.1 is implemented as an advisory release under the existing product-owner exception. It does
not change the G3 measurements in `r3.4-release-evidence.md`, claim commercial validation, contact
a supplier, or create, submit, approve, or send a purchase request or purchase order.

## Delivered Contract

The existing `GET /api/v1/suppliers/{supplier_id}/scorecard` remains v1-compatible and attaches
the latest persisted v2 detail when available. Seven new routes provide recomputation, latest-risk
listing, brief preparation/list/detail, and append-only acknowledge/dismiss actions. Together,
OpenAPI exposes all eight approved R4.1 route/method pairs.

- Active tenant members may read scorecards, risk snapshots, and briefs.
- Only owners and buyers may recompute, prepare, acknowledge, or dismiss.
- Mutations require a UUID `Idempotency-Key` at runtime and append an audit event.
- Cross-tenant identifiers are indistinguishable from unknown identifiers (`404 not_found`).
- Lists use opaque cursors with a default of 50 and maximum of 100.
- Every v2 response exposes `release_posture = "g3_unmet"`.

## Business Validation

The deterministic risk model uses a 180-day window split into equal baseline/current halves and
`Decimal` arithmetic throughout. The four components are concentration (30%), price drift (25%),
reliability (25%), and single-source exposure (20%). Exactly three available components are
renormalized; fewer than three suppress the score. Risk bands are low below 0.35, medium from 0.35
to below 0.65, and high at or above 0.65.

Concentration stays in currency buckets. Price drift compares product/unit/currency medians and
excludes zero baselines. Reliability includes due non-cancelled orders, uses cumulative line
receipts, reports exclusions, and measures current failure plus decay. Single-source exposure
requires a compatible active alternative for the same normalized product, unit, and currency.

Briefs contain at most one deterministic item in each category: price trajectory, alternatives,
service performance, concentration/volume, payment context, and purchase pattern. Calculations,
validity, confidence, explicit money currencies, localized question keys, and typed source links
are stored or returned; generated prose is not used. Missing due dates or provider balances remain
unavailable rather than inferred.

## Data and Isolation

Migration `20260920000009_supplier_iq_v2.sql` extends the existing snapshot and synced-bill rows
and adds normalized metric, evidence, brief, item, item-evidence, and action tables. All new tables
have forced RLS, tenant-pinned foreign keys, select `USING` and insert `WITH CHECK` policies, and
no update/delete/truncate grants. Snapshot, brief, and action writes are transactional; evidence
links are typed and each brief item must have evidence before commit.

Hosted migration and isolation verification were completed earlier in the R4.1 workstream:

- Hosted Supplier IQ v2 persistence/rollback/tenant suite: 3/3 passed.
- Dedicated R4.1 isolation suite: 12/12 passed.
- Full hosted isolation suite: 96/99 passed; the three repeatable failures are pre-existing
  privilege assertions for platform invitations, push notifications, and supplier commercial
  terms and do not touch R4.1 tables.

## Verification Record

Completed on 2026-09-21:

| Gate | Result |
|---|---|
| Supplier IQ v2, brief, and v1 metric unit/contract coverage | 78 passed |
| Hosted-only focused tests in worktree without `TEST_DATABASE_URL` | 16 skipped |
| Ruff, offers + accounting modules | Passed |
| Focused Supplier Risk Angular tests | 31 passed |
| Supplier Risk Playwright journey | 3 passed |
| Full accessibility gate, English/Arabic/RTL/keyboard | 114 passed |
| Angular lint | Passed |
| Angular production build | Passed with pre-existing unrelated warnings |
| English/Arabic Supplier Risk catalogue key parity | Exact |

The root `test:a11y` script was corrected to pass the Playwright grep option instead of launching
all E2E tests. Five stale POS title assertions were aligned with the current translation catalogues;
the POS accessibility subset then passed 8/8 and the complete accessibility gate passed 114/114.

## Pending Final Checks

Re-run on 2026-09-23 from `main` (commit `ab4a8fc`, after the R4.0/R4.1 CI-fix pass):

| Gate | Result |
|---|---|
| Full API suite (`uv run --project apps/api --with-editable services/extraction-worker pytest apps/api -q`, repo root, matching CI exactly) | 1576 passed, 8 failed — all 8 are the pre-existing `azure` SDK / Supabase Storage local-environment gaps unrelated to R4.0/R4.1 |
| Tenant isolation (`pnpm test:isolation`) | 99/99 passed — improved from the 96/99 recorded above; the three previously-repeatable failures (platform invitations, push notifications, supplier commercial terms) are now also passing |
| Angular unit tests (`ng test --watch=false`) | 573/573 passed |
| Angular production build (`ng build --configuration production`) | Passed with the same pre-existing unrelated SCSS budget warnings noted above |
| Hosted `GET /api/v1/health` | `200 {"status":"ok"}` |
| Hosted unauthenticated 404 envelope | Confirmed `{"code":"not_found","message":"Resource was not found.","details":null,"trace_id":"..."}` shape |

Live hosted walkthrough completed 2026-09-23 against `procurepilot.iron-sys.com`, signed in as
the isolated `walkthrough.owner@example.test` test tenant (Owner role):

| Check | Result |
|---|---|
| Sign-in, tenant isolation banner, RBAC role display | Correct |
| `Supplier Risk` — `Recompute risk` (`POST /api/v1/supplier-iq/recompute`) | Succeeded; "Recomputed 1 supplier snapshot(s)"; `G3 unmet` posture shown |
| Supplier scorecard detail (v2, all 4 components) | Renders correctly; "Insufficient Evidence" state with 0 samples per component, consistent with the `>= 3` evidentiary gate `supplier_iq_v2.py` enforces (see the `test_negotiation_briefs.py` fixture fix above) |
| `Reorder Forecasts` — `Refresh forecasts` (`POST /forecasting/recompute`) | Succeeded; clean "No reorder proposals yet" empty state, no errors |
| English/Arabic desktop screenshots (Supplier Risk, Reorder Forecasts) | Captured; Arabic renders full RTL translation with no layout breakage |
| English/Arabic mobile screenshots (390×844, Supplier Risk, Reorder Forecasts) | Captured; no overflow, RTL mirrors correctly on both pages |
| Locale reset to English before sign-out | Done, to avoid leaking Arabic into the shared test account per known session-persistence behavior |

Full authenticated prepare/list/get/acknowledge/dismiss brief actions were not separately
exercised live: `Acme Global Supplies` (the only supplier on this test tenant) has no purchase
history and therefore never clears the evidentiary gate, so the negotiation-brief precondition is
never met on this tenant as-is. That exact flow — prepare, replay, list, get, acknowledge, dismiss,
with append-only evidence assertions — is already covered end-to-end by
`test_negotiation_briefs.py` against a real RLS-enforced Postgres.

These items remain evidence work only. G3 stays unmet regardless of their outcome.
