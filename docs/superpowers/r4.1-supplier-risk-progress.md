# R4.1 Supplier IQ v2 Progress

| Task | Status | Commit |
|---:|---|---|
| 1. Data model and RLS | reviewed + runtime verified | 2c91482 |
| 2. Provider payment evidence | reviewed + unit verified | 07b3437 |
| 3. Pure risk calculator | reviewed + unit verified | 879771f |
| 4. Atomic scorecard persistence | reviewed + hosted integration verified | dd09f90 |
| 5. Negotiation briefs | implemented: persisted briefs, evidence links, replay, actions | 6631328 |
| 6. API contract | implemented: typed routes, RBAC, idempotency, OpenAPI checks | 45fe957 |
| 7. Angular API client | implemented: strict types, bounded cursors, idempotent mutations | eb046c1, 189b7d7 |
| 8. Supplier Risk queue | implemented: scan table, RBAC recompute, stale/G3 states, RTL | af3cfbf |
| 9. Scorecard and brief detail | implemented: v2 evidence panel and human-reviewed brief flow | d602010, ca3c986 |
| 10. i18n, RTL, accessibility, E2E | verified: keyboard journey, mobile RTL, catalogue parity, axe | this commit |
| 11. Documentation and release verification | queued | |

## Needs your eyes

- R4.1 remains an implementation exception; G3 is not being marked passed.
- Frontend implementation is delegated to Agy when practical and independently reviewed.
- Task 1 hosted migration applied; dedicated isolation suite passed 12/12.
- Full hosted tenant-isolation suite passed 96/99; three repeatable pre-existing privilege
  assertion failures remain for platform invitations, push notifications, and supplier commercial
  terms. The failures are outside the R4.1 files and reproduce when selected directly.
- The v1 Supplier IQ integration suite is currently blocked before assertions by the hosted
  PgBouncer prepared-statement collision (`DuplicatePreparedStatement`); direct targetless
  `ON CONFLICT` compatibility passed against the migrated schema.
- Task 2 focused accounting suite passed 110/110; provider parsing, sync persistence, and
  synced-bill projection are covered. Database accounting integration and live-provider calls
  remain unrun.
- Task 4 hosted Supplier IQ v2 integration passed 3/3: durable replay, atomic rollback, typed
  evidence, active-supplier filtering, opaque pagination, and two-tenant isolation. Unit v2 plus
  existing metrics tests passed 73/73. The hosted recompute idempotency migration is applied.
- Task 5 now includes database-backed prepare/list/get/actions, immutable evidence links, replay,
  append-only decisions, and a hosted integration test (skipped locally without
  `TEST_DATABASE_URL`).
- Task 6 exposes all eight approved Supplier IQ v2 and negotiation-brief routes with typed
  responses, RBAC, UUID idempotency headers, stable cursor pagination, and contract coverage.
  G3 remains unmet and no purchasing side effect is introduced.
- Task 7 returns supplier names and risk levels directly in the latest-snapshot list, avoiding
  per-row supplier lookups. Limits default to 50 and remain capped at 100.
- Task 8 adds the all-member Supplier Risk queue with explicit low/medium/high/insufficient,
  confidence, evidence age, top driver, stale validity, and `g3_unmet` posture. Only owners and
  buyers can recompute; the queue links to the existing scorecard and cannot trigger purchasing.
  Focused Angular tests passed 15/15, lint passed, and the production build completed with only
  pre-existing warnings in unrelated components.
- Task 9's pre-implementation review found that the existing scorecard endpoint still returned
  only v1 data. The compatibility contract now preserves every v1 field while attaching the latest
  persisted v2 components, currency buckets, exclusions, validity, evidence references, risk level,
  and G3 posture. Negotiation brief items now expose typed source targets for evidence navigation.
- The scorecard embeds a focused v2 evidence panel and prepares briefs only for current sufficient
  or provisional snapshots. Brief detail keeps calculations and evidence before localized questions;
  acknowledgement and dismissal remain role-gated append-only human decisions with no purchasing
  side effect.
- Task 10 adds accessible names for risk tables and source links, exact English/Arabic key parity,
  keyboard navigation from queue to scorecard to brief, and mobile RTL overflow coverage. The
  focused Supplier Risk journey passed 3/3, focused Angular tests passed 31/31, lint and production
  build passed, and the repaired root accessibility gate passed 114/114. The root script now sends
  Playwright's grep arguments correctly; five stale POS title expectations were aligned with the
  current translation catalogues so those axe scans execute again.
- Full API suite passed 961/961 runnable tests with 596 hosted-database tests skipped because this
  worktree has no `TEST_DATABASE_URL`. Repo-wide pytest collection also needs the optional worker
  environments (`boto3`, Azure Document Intelligence, and optimiser package) that are not installed.

## End-of-run checklist

- Run full API, web, isolation, accessibility, migration, and physical API checks.
- Perform final coherence review and decide whether to push/open the PR.
