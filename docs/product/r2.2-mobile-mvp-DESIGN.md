---
version: alpha
name: ProcurePilot Mobile
description: >
  R2.2 Mobile MVP design system — Material Design 3, light theme, bilingual
  English/Arabic (RTL). Scoped to specs/009-mobile-app-mvp/spec.md.
colors:
  primary: "#4F46E5"
  onPrimary: "#FFFFFF"
  secondary: "#5F6368"
  tertiary: "#F59E0B"
  onTertiary: "#1A1C1E"
  success: "#10B981"
  onSuccess: "#FFFFFF"
  error: "#EF4444"
  onError: "#FFFFFF"
  neutral: "#F7F7F8"
  surface: "#FFFFFF"
  textPrimary: "#1A1C1E"
  textSecondary: "#5F6368"
  border: "#E3E3E6"
  statusDraft: "{colors.secondary}"
  statusSubmitted: "{colors.primary}"
  statusApproved: "{colors.success}"
  statusRejected: "{colors.error}"
  statusWithdrawn: "{colors.secondary}"
  statusQueued: "{colors.tertiary}"
typography:
  headline:
    fontFamily: Roboto
    fontSize: 1.5rem
    fontWeight: 600
    lineHeight: 1.3
  headline-ar:
    fontFamily: Noto Sans Arabic
    fontSize: 1.5rem
    fontWeight: 600
    lineHeight: 1.4
  title:
    fontFamily: Roboto
    fontSize: 1.125rem
    fontWeight: 600
    lineHeight: 1.3
  title-ar:
    fontFamily: Noto Sans Arabic
    fontSize: 1.125rem
    fontWeight: 600
    lineHeight: 1.4
  body:
    fontFamily: Roboto
    fontSize: 0.875rem
    fontWeight: 400
    lineHeight: 1.5
  body-ar:
    fontFamily: Noto Sans Arabic
    fontSize: 0.875rem
    fontWeight: 400
    lineHeight: 1.6
  label:
    fontFamily: Roboto
    fontSize: 0.75rem
    fontWeight: 500
    lineHeight: 1.3
    letterSpacing: 0.02em
  label-ar:
    fontFamily: Noto Sans Arabic
    fontSize: 0.75rem
    fontWeight: 500
    lineHeight: 1.4
rounded:
  sm: 8px
  md: 12px
  lg: 16px
  pill: 999px
spacing:
  xs: 4px
  sm: 8px
  md: 16px
  lg: 24px
  xl: 32px
components:
  buttonPrimary:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.onPrimary}"
    typography: "{typography.label}"
    rounded: "{rounded.md}"
    height: 48px
  buttonSecondary:
    backgroundColor: transparent
    textColor: "{colors.primary}"
    typography: "{typography.label}"
    rounded: "{rounded.md}"
    height: 48px
  buttonText:
    backgroundColor: transparent
    textColor: "{colors.primary}"
    typography: "{typography.label}"
    height: 48px
  cardHero:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.onPrimary}"
    rounded: "{rounded.md}"
    padding: "{spacing.md}"
  cardOutlined:
    backgroundColor: "{colors.surface}"
    rounded: "{rounded.md}"
    padding: "{spacing.md}"
  listTile:
    backgroundColor: "{colors.surface}"
    padding: "{spacing.sm}"
  statusChipDraft:
    backgroundColor: "{colors.statusDraft}"
    textColor: "{colors.onPrimary}"
    typography: "{typography.label}"
    rounded: "{rounded.pill}"
  statusChipSubmitted:
    backgroundColor: "{colors.statusSubmitted}"
    textColor: "{colors.onPrimary}"
    typography: "{typography.label}"
    rounded: "{rounded.pill}"
  statusChipApproved:
    backgroundColor: "{colors.statusApproved}"
    textColor: "{colors.onSuccess}"
    typography: "{typography.label}"
    rounded: "{rounded.pill}"
  statusChipRejected:
    backgroundColor: "{colors.statusRejected}"
    textColor: "{colors.onError}"
    typography: "{typography.label}"
    rounded: "{rounded.pill}"
  statusChipWithdrawn:
    backgroundColor: "{colors.statusWithdrawn}"
    textColor: "{colors.onPrimary}"
    typography: "{typography.label}"
    rounded: "{rounded.pill}"
  statusChipQueued:
    backgroundColor: "{colors.statusQueued}"
    textColor: "{colors.onTertiary}"
    typography: "{typography.label}"
    rounded: "{rounded.pill}"
  topAppBar:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.textPrimary}"
    height: 56px
  bottomNav:
    backgroundColor: "{colors.surface}"
    height: 64px
  textField:
    backgroundColor: "{colors.surface}"
    rounded: "{rounded.sm}"
    height: 48px
    padding: "{spacing.sm}"
---

## Overview

ProcurePilot Mobile is B2B procurement software, not a consumer app: clean, trustworthy, and
efficient. Every screen exists to get a branch manager through a real-world task — restocking, a
quick signal, a status check — as fast as possible, or to give an approver a glanceable read on
what's waiting for them. Material Design 3, light theme only for this release, flat and
outlined over heavy shadows, fully bilingual (English / Arabic RTL) from the same components,
built to WCAG 2.1 AA.

## Colors

- **Primary (`#4F46E5`, indigo):** filled buttons, active navigation state, links, the
  Submitted status.
- **Secondary (`#5F6368`, slate):** body/metadata text, borders, Draft and Withdrawn statuses.
- **Tertiary (`#F59E0B`, amber):** the single warning/highlight color — also used for the
  Queued (offline, not yet synced) status, so "awaiting" states share one visual language.
- **Success (`#10B981`, green):** the Approved status only.
- **Error (`#EF4444`, red):** the Rejected status, destructive actions (e.g. sign out text).
- **Neutral (`#F7F7F8`) / Surface (`#FFFFFF`):** screen background vs. card/tile background —
  kept close in value on purpose, this is a calm, low-contrast-between-surfaces app, contrast is
  spent on text and status, not on separating panels.
- Status color is never the only signal — every status chip pairs its color with a distinct icon
  and a text label (see Components).

## Typography

Roboto for English, Noto Sans Arabic for Arabic — never Roboto for Arabic text, it has no Arabic
glyph coverage. Four sizes, each with a Latin and an Arabic variant: `headline` (screen titles),
`title` (card and section headers), `body` (default text, the 14sp floor this app never goes
below for anything a user reads as a sentence), `label` (buttons, chips, badges — the only text
allowed to go as small as 12sp). Arabic line-heights run slightly taller than their Latin
counterparts to accommodate the script.

## Layout

8px spacing grid (`spacing.xs` through `spacing.xl`), 16px standard screen padding, 48×48px
minimum interactive touch target on every tappable element (Material 3's real minimum, not iOS's
44). RTL is not an afterthought: every layout uses logical start/end, never left/right, and every
directional element (back arrows, chevrons, the top app bar's leading icon) mirrors in the Arabic
variant.

## Elevation & Depth

Flat and outlined by default — this is enterprise software, not a skeuomorphic consumer app. One
elevation level only, reserved for things that genuinely lift off the page: dialogs and bottom
sheets. Cards, list tiles, and the bottom nav sit flush with the surface beneath them; use a hairline
`{colors.border}` divider instead of a shadow to separate content where needed.

## Shapes

`rounded.sm` (8px) for text fields and small controls, `rounded.md` (12px) for cards and buttons —
rounded but not playful — `rounded.lg` (16px) for sheets and dialogs, `rounded.pill` for every
status chip.

## Components

- **Buttons** — `buttonPrimary` (filled indigo, the one clear call-to-action per screen),
  `buttonSecondary` (outlined indigo), `buttonText` (text-only, for tertiary actions like "Not
  now" or "Use password instead"). Same height and corner radius across all three.
- **Status chip** — six variants, one per `Purchase Request` status plus the cross-cutting
  Queued state: `statusChipDraft`, `statusChipSubmitted`, `statusChipApproved`,
  `statusChipRejected`, `statusChipWithdrawn`, `statusChipQueued`. Each pairs its color with an
  icon (pencil / clock / check / X / undo / sync) inside the pill — color is reinforcement, the
  icon and label carry the meaning.
- **Cards** — `cardHero` (filled primary, used for exactly one action per screen — "New
  request") and `cardOutlined` (everything else, secondary actions and content cards).
- **List tile** — leading icon, title, optional subtitle, optional trailing chip or switch.
- **Top app bar** — leading back/close icon, title; mirrors fully for RTL.
- **Bottom navigation** — three items only for this release (Home, Requests, Settings); outlined
  icon inactive, filled icon with primary-color tint active.
- **Text field** — outlined, with label/hint/error states.

## Do's and Don'ts

- **Do** pair every status indicator with an icon and a label. **Don't** rely on color alone for
  status — including on the low-stock report's stock level, which this release doesn't have; it's
  a one-tap signal, not an inventory reading.
- **Do** keep exactly one `cardHero` per screen (the single primary action). **Don't** let a
  secondary card compete with it in size or color weight.
- **Do** mirror every directional icon and use logical start/end spacing for Arabic. **Don't**
  hardcode left/right positioning anywhere.
- **Do** show buttons in their normal idle state in a static mockup. **Don't** depict a
  loading/spinner state as a screen's primary frame — that's a transient micro-interaction, note
  it separately if needed.
- **Don't** add a camera icon, barcode/scan control, or photo-attachment element to any R2.2
  screen — camera capture is a later release; every field in this release's request and
  low-stock flows is text/number/date input only.
- **Don't** add an approve/reject action anywhere on mobile in this release — the approver's home
  card is a read-only stat (large number + label), never a button, chip, or chevron.
