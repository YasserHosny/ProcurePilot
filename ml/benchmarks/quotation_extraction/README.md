# Quotation Extraction Benchmarks

Versioned held-out quotation extraction benchmarks belong here.

The current repository intentionally contains no real benchmark set because the active provider is a
deterministic stub until Bedrock/Azure Document Intelligence credentials are available. Any tiny
sample used by `ml/evals/quotation_extraction/run_eval.py` is a placeholder smoke fixture only and
must not be used to claim the 90% field-accuracy gate or calibration gate.
