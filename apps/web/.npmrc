# Angular's build pipeline resolves deep subpaths (e.g. @angular/animations/browser) from inside
# other @angular packages. pnpm's default strict, symlinked node_modules hides those from each
# other and the build fails to resolve them. A hoisted layout matches what Angular expects.
node-linker=hoisted
