[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "procurepilot-logging"
version = "0.1.0"
description = "Shared structured JSON logging for ProcurePilot services"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/procurepilot_logging"]
